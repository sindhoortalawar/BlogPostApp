
<nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
    <div class="container-fluid">
        <a class="navbar-brand" asp-controller="Post" asp-action="Index">BloggingApp</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link active" asp-controller="Post" asp-action="Index">
                        Home
                        <span class="visually-hidden">(current)</span>
                    </a>
                </li>

                @if(User.Identity.IsAuthenticated)
                {
                    @if(User.IsInRole("PostOwner") || User.IsInRole("Admin"))
                    {
                        <li class="nav-item">
                            <a class="nav-link" asp-controller="Post"  asp-action="MyBlogs" >My Blogs</a>
                        </li>
                    }
                    else if(User.IsInRole("Admin"))
                    {
                        <liv class="nav-item">
                            <a asp-controller="Auth" asp-action="GetUsers">Manage Users</a>
                        </liv>
                        <li class="nav-item">
                            <a asp-controller="Post" asp-action="Index">Manage Blogs</a>
                        </li>
                    }
                    <li class="nav-item">
                        <a class="nav-link" asp-controller="Auth"  asp-action="Profile" >Profile</a>
                    </li>

                    <li class="nav-item">
                    <a asp-action="ChangePassword" asp-controller="Auth"
                       class="nav-link">
                        Change Password
                    </a>
                    </li>
                }
                @*
                <li class="nav-item">
                    <a class="nav-link" asp-controller="Post" asp-action="Index">Posts</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" >Pricing</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" >About</a>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Dropdown</a>
                    <div class="dropdown-menu">
                        <a class="dropdown-item" >Action</a>
                        <a class="dropdown-item" >Another action</a>
                        <a class="dropdown-item" >Something else here</a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" >Separated link</a>
                    </div>
                </li>
                *@
            </ul> 

            @await Component.InvokeAsync("NavbarUser")

            @* <form class="d-flex">
                <input class="form-control me-sm-2" type="search" placeholder="Search">
                <button class="btn btn-secondary my-2 my-sm-0" type="submit">Search</button>
            </form> 
            *@

            @* @if(User.Identity.IsAuthenticated)
            {
                <div class="row">
                    <div class="col-6">
                         <li class="nav-item me-3 text-muted small"> 
                            <img src="@User.FindFirst("Avatar")?.Value" alt="Profile" class="rounded-circle border me-1" style="width:30px; height:30px; object-fit:cover;" />
                            <span class="fw-semibold">@User.FindFirst("Email")?.Value</span>
                         </li> 
                    </div>
                    <div class="text-end col-6">
                        <a asp-controller="Auth" asp-action="Logout" class="btn btn-secondary p-2">Logout</a>
                    </div>
                </div>
            }
            else
            {
                <div class="text-end">
                    <a asp-action="Login" asp-controller="Auth" class="btn btn-dark p-2 me-1">Login</a>
                    <a asp-action="Register" asp-controller="Auth" class="btn btn-dark ms-1 p-2">Register</a>
                </div>
            } *@


            @* @if (User.Identity.IsAuthenticated)
            {
                <ul class="navbar-nav ms-auto align-items-center">
                    <li class="nav-item me-3 text-muted small">
                        <img src="@User.FindFirst("Avatar")?.Value" alt="Profile" class="rounded-circle border me-1" style="width:30px; height:30px; object-fit:cover;" />
                        <span class="fw-semibold">@User.FindFirst("FullName")?.Value</span>
                    </li>
        
                    <li class="nav-item">
                        <form asp-controller="Auth" asp-action="Logout" method="post" class="d-inline">
                            @Html.AntiForgeryToken()
                            <button type="submit" class="btn btn-outline-danger btn-sm px-3 fw-semibold rounded-pill">
                                <i class="bi bi-box-arrow-right me-1"></i> Sign Out
                            </button>
                        </form>
                    </li>
                </ul>
            }
            else
            {
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link btn bg-secondary fw-semibold text-white p-2" asp-controller="Auth" asp-action="Login">Login</a>
                    </li>
                </ul>
            } *@
            
        </div>
    </div>
</nav>